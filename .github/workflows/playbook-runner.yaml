name: Run Ansible Playbook for add user
on:
 push:
   branches: [main]
 pull_request:
   branches: [main]
 workflow_dispatch:
  

jobs:
  ansible-playbook:
    runs-on: [ ansible-master ]  # Change this if you're not using a self-hosted runner

    steps:
      - uses: actions/checkout@v4

      - name: Execute Ansible Playbook
     
        run: |  
           ansible-playbook  playbook1-useradd.yaml -i /home/lourde/ansible/inventory.yaml --become
