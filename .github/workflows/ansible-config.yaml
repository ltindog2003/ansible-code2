name:  Configuration ansible.cfg 

on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u lourde bash -c 'cat > /home/lourde/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory     = /home/lourde/ansible/inventory.yaml
          roles_path    = /home/lourde/ansible/roles
          host_key_checking = False
          remote_user = lourde
          private_key_file = /home/lourde/.ssh/id_rsa
          log_path = /home/lourde/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/lourde/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/lourde/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown lourde:lourde /home/lourde/ansible/ansible.cfg
          sudo chmod 644 /home/lourde/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u lourde ls -la /home/lourde/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u lourde cat /home/lourde/ansible/ansible.cfg
